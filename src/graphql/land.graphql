# Hey Emacs, this is -*- coding: utf-8 -*-

type Element {
  id: String!
  name: String!
  description: String!
  processes: [String]!
  alternatives: [Element!]
}

type Query {
  element(
    id: String!
  ): Element

  elementIds: [String!]
}

schema {
  query: Query
}
